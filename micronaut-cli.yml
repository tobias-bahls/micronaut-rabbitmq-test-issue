applicationType: default
defaultPackage: example.micronaut
testFramework: junit
sourceLanguage: kotlin
buildTool: gradle_kotlin
features: [app-name, gradle, http-client-test, jackson-databind, junit, kotlin, kotlin-application, ksp, logback, micronaut-aot, micronaut-build, netty-server, rabbitmq, readme, shade, test-resources, validation, yaml, yaml-build]
